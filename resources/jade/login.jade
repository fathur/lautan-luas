extends ./_layout.jade

block title
  title Investor Relations

block content
  .container-fluid.hero

  //- Breadcrumb
  .breadcrumb-wrapper
    .container
      ol.breadcrumb
        li
          a(href="index.html") Home
        li.active Corporate Social Responsibility

    //- Main content
  .padding-extra-large.white-bg
    .container
      .row
        .col-sm-6
          h2 Log In
          p If you already have an account, please log in below.
          form(action="#" method="post")
            .form-group
              input.form-control.input-lg#email(type="text" placeholder="Email")
            .form-group
              input.form-control.input-lg#password(type="text" placeholder="Password")
            button.btn.btn-transparant(type="submit") Log In
            a(href="#") Forgot Password
        .col-sm-6
          h2 Sign Up
          p Don’t have an account yet? Sign up now.
          form(action="login-thanks.html" method="post")
            .form-group
              input.form-control.input-lg#email(type="text" placeholder="Email")
            .form-group
              input.form-control.input-lg#password(type="text" placeholder="Password")
            .form-group
              input.form-control.input-lg#confirm-password(type="text" placeholder="Confirm Password")
            button.btn.btn-transparant(type="submit") Sign Up
            